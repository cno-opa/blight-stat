# requirements.R
# Everything you need to make a cake
#
#

require(lubridate)
require(zoo)
require(dplyr)
require(ggplot2)
require(scales)
require(reshape2)
require(maps)
require(maptools)
require(sp)
require(rgdal)
require(jsonlite)
require(dplyr)
require(rgeos)
require(RColorBrewer)
require(classInt)
require(ggmap)

library("grid")
library("gridExtra")
library("gtable")
